export function capitalizeWord(texto) {
	return texto.charAt(0).toUpperCase() + texto.slice(1).toLowerCase();
}

export function capitalizeFirstWord(frase) {
  return frase.charAt(0).toUpperCase() + frase.slice(1);
}