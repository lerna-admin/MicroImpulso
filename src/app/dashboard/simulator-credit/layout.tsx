// app/dashboard/simulator-credit/layout.tsx
export const metadata = {
  title: 'Loan Simulation | Dashboard',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
